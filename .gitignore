# Development / AI-assisted working files — not shipped with the plugin.
temp_for_ai/
.claude/

# Node / build tooling (if added later).
node_modules/

# OS files.
Thumbs.db
.DS_Store

# Editor files.
*.swp
*.swo
*~
.vscode/
.idea/

# Moodle cache (should never be committed).
moodle/.cache/

# Composer dev tooling (phpcs) - not shipped with the plugin.
vendor/
composer.lock
